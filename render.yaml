services:
  - type: web
    name: nitedu-anomaly-detection-ml
    env: python
    buildCommand: "pip install fastapi uvicorn torch scikit-learn joblib numpy pandas python-multipart python-dotenv"
    startCommand: "cd backend && python -m uvicorn app.main_simple:app --host 0.0.0.0 --port $PORT"
    plan: free
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        value: sqlite:///tmp/cyberdefense.db
      - key: ML_MODEL_PATH
        value: /opt/render/project/src/data/models